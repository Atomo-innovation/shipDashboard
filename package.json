{
  "name": "ship-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node local-server.js",
    "dev": "node local-server.js",
    "build": "mkdir -p .vercel/output/static && rm -rf .vercel/output/static/* && cp -R public/. .vercel/output/static/ && printf '%s\\n' '{\"version\":3,\"routes\":[{\"handle\":\"filesystem\"},{\"src\":\"/twin\",\"dest\":\"/three_realistic.html\"}]}' > .vercel/output/config.json"
  },
  "description": "Ship digital twin dashboard",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1"
  }
}
